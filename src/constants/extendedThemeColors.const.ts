import { DEFAULT_THEME_COLORS } from '@forgedevstack/aerocraft';

export const PINK_SCALE = DEFAULT_THEME_COLORS.pink as Record<string, string>;
export const RED_SCALE = DEFAULT_THEME_COLORS.red as Record<string, string>;
export const YELLOW_SCALE = DEFAULT_THEME_COLORS.yellow as Record<string, string>;
export const BLUE_SCALE = DEFAULT_THEME_COLORS.blue as Record<string, string>;

export const DOCS_PALETTE_STEP_KEYS = [
  25, 50, 100, 200, 300, 400, 500, 600, 700, 800, 900, 950,
] as const;
